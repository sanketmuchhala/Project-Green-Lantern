{
  "name": "byok-research-copilot",
  "version": "1.0.0",
  "description": "Local-only BYOK Research Copilot with your own API keys",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "concurrently \"npm -C server run dev\" \"npm -C web run dev\"",
    "build": "npm -C server run build && npm -C web run build",
    "start": "npm -C server start",
    "clean": "rm -rf server/dist web/dist node_modules server/node_modules web/node_modules"
  },
  "workspaces": [
    "server",
    "web"
  ],
  "devDependencies": {
    "concurrently": "^8.2.2",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  }
}
